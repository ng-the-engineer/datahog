{
  "name": "aggregation-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "babel --presets es2015 -d lib/ src",
    "start": "node lib/app.js",
    "api-tests": "babel --presets es2015 -d lib/ src && mocha lib/api-tests/*.spec.js --exit",
    "unit-tests": "babel --presets es2015 -d lib/ src && mocha --require babel-register lib/services/*.spec.js --unhandled-rejections=strict"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.20.0",
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "dynamodb": "^1.2.1",
    "koa": "^2.13.0",
    "koa-bodyparser": "^4.3.0",
    "koa-logger": "^3.2.1",
    "koa-router": "^9.4.0"
  },
  "devDependencies": {
    "babel-register": "^6.26.0",
    "chai": "^4.2.0",
    "chai-http": "^4.3.0"
  }
}
